# Подстановки в innerHTML, требующие внимания, на момент фиксации.
# Файл нужен защёлке: новая запись валит сборку, старые чинятся
# постепенно. Уменьшать список можно и нужно; увеличивать — только
# осознанно, вместе с объяснением в коммите.
#
# Пересобрать: python tools/audit_innerhtml.py --baseline

admin-screens.js	allReviews.map(r => `
admin-screens.js	s.done
admin-screens.js	s.errors ? `, ошибок: ${s.errors}` : ''
admin-screens.js	s.indexed_pages
admin-screens.js	safeTag
admin-screens.js	sensitiveNonce()
admin-screens.js	state.books.length
admin-screens.js	state.books.map(b => `
admin-screens.js	stats.total_books
admin-screens.js	stats.total_downloads
admin-screens.js	stats.total_quiz_attempts
admin-screens.js	stats.total_reviews
admin-screens.js	stats.total_users
admin-screens.js	stats.total_views
admin-screens.js	statusColor[item.status]
admin-screens.js	statusIcon[item.status]
admin-screens.js	u.department ? `<div data-static-style="a193">${eh(u.department)}</div>` : ''
app.js	(() => { const stage = findKillChainStageForBook(b); if (!stage) return ''; return `<button class="btn-detail" data-stat
app.js	ICONS[tab.icon]
app.js	[2, 3, 4].map(n => `
app.js	adminStats
app.js	b.has_cover ? `<img src="${api.books.coverUrl(b.id)}" alt="" data-static-style="a467" data-onerror="replaceWithFallback(
app.js	b.has_file ? (offlineBookIds.has(b.id) ? `<button class="btn-detail offline-btn-saved" data-onclick="removeBookOffline($
app.js	b.rating
app.js	bookCategoriesText(b)
app.js	btn('csv', 'CSV', 'Таблица для Excel')
app.js	btn('json', 'JSON', 'Для импорта в другие приложения')
app.js	btn('md', 'Markdown', 'Текст с разметкой')
app.js	btn('pdf', 'PDF документ', 'Для печати и чтения')
app.js	c.score
app.js	card(activeDays, 'активных дней', '#10b981')
app.js	card(avgQuiz !== null ? avgQuiz + '%' : '—', 'средний балл тестов', '#f59e0b')
app.js	card(hours + ' ч', 'примерно времени', '#a855f7')
app.js	card(totalPages, 'страниц прочитано', 'var(--accent)')
app.js	dateStr ? `<div data-static-style="a575">Тест пройден: ${dateStr}</div>` : ''
app.js	encodeURIComponent(c.category).replace(/'/g, '')
app.js	formatBytes(stats.cacheSize)
app.js	formatBytes(stats.quota)
app.js	formatBytes(stats.used)
app.js	formatLabel
app.js	hasLevel ? `
app.js	html
app.js	info.icon.replace('width="22"','width="40"').replace('height="22"','height="40"').replace('width="20"','width="40"').rep
app.js	inner
app.js	isAdmin ? `<button class="btn-detail" data-static-style="a469" data-onclick="openAdminBookModal(${b.id})">${ICONS.settin
app.js	options.map(opt => `
app.js	pdfCurrentPage
app.js	pdfTotalPages
app.js	res.hits.map(h => `
app.js	res.total
app.js	result.overall_percentage
app.js	sensitiveNonce()
app.js	stats.cacheCount
app.js	tab ? tab.label : ''
app.js	tab.label
app.js	topicsHtml
app.js	usedPct
app.js	weakHtml
app.js	weeks.map(w => `<div title="${w} стр." style="flex:1
ar-schemes.js	(stage.defenseTools && stage.defenseTools.length) ? `
ar-schemes.js	AR_3D.pyramid
ar-schemes.js	arrows
ar-schemes.js	cards
ar-schemes.js	circles
ar-schemes.js	cx
ar-schemes.js	cy
ar-schemes.js	cy + 14
ar-schemes.js	cy - 6
ar-schemes.js	items
ar-schemes.js	labels
ar-schemes.js	layers
ar-schemes.js	percent
ar-schemes.js	rInner - 6
ar-schemes.js	segs
ar-schemes.js	stage.attacker.map(a => `<li data-static-style="a087">${eh(a)}</li>`).join('')
ar-schemes.js	stage.defender.map(d => `<li data-static-style="a087">${eh(d)}</li>`).join('')
ar-schemes.js	stage.defenseMethod ? eh(stage.defenseMethod.code + ' — ' + stage.defenseMethod.nameRu) : '—'
ar-schemes.js	stage.defenseMethod ? stage.defenseMethod.code.charAt(0) : '?'
ar-schemes.js	stage.defenseMethod ? stage.defenseMethod.color : '#666'
ar-schemes.js	stage.defenseMethod ? stage.defenseMethod.color : '#fff'
ar-schemes.js	stage.nameRu.toUpperCase()
ar-schemes.js	stages.map((s) => { const color = (s.defenseMethod && s.defenseMethod.color) || '#3b82f6'; const badge = (s.code || Stri
ar-schemes.js	stages.map((s, i) => { const color = colors[i] || '#3b82f6'; return `<button data-onclick="selectKillChainStage(${s.id})
assistant-chat.js	c.message_count
assistant-chat.js	sensitiveNonce()
