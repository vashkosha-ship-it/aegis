# Подстановки в innerHTML, требующие внимания, на момент фиксации.
# Файл нужен защёлке: новая запись валит сборку, старые чинятся
# постепенно. Уменьшать список можно и нужно; увеличивать — только
# осознанно, вместе с объяснением в коммите.
#
# Пересобрать: python tools/audit_innerhtml.py --baseline

account-settings.js	card(activeDays, 'активных дней', '#10b981')
account-settings.js	card(avgQuiz !== null ? avgQuiz + '%' : '—', 'средний балл тестов', '#f59e0b')
account-settings.js	card(hours + ' ч', 'примерно времени', '#a855f7')
account-settings.js	card(totalPages, 'страниц прочитано', 'var(--accent)')
account-settings.js	formatBytes(stats.used)
account-settings.js	html
account-settings.js	options.map(opt => `
account-settings.js	topCats.length ? topCats.map(([cat, n]) => { const pct = Math.round(n / topCats[0][1] * 100); return `<div data-static-s
account-settings.js	weeks.map(w => `<div title="${w} стр." data-dynamic-style="${dynamicStyleToken`flex:1;min-width:4px;height:${Math.max(4,
admin-screens.js	sensitiveNonce()
ar-schemes.js	AR_3D.pyramid
ar-schemes.js	arrows
ar-schemes.js	cards
ar-schemes.js	circles
ar-schemes.js	cx
ar-schemes.js	cy
ar-schemes.js	cy + 14
ar-schemes.js	cy - 6
ar-schemes.js	items
ar-schemes.js	labels
ar-schemes.js	layers
ar-schemes.js	percent
ar-schemes.js	rInner - 6
ar-schemes.js	segs
ar-schemes.js	stages.map((s) => { const color = (s.defenseMethod && s.defenseMethod.color) || '#3b82f6'; const badge = (s.code || Stri
ar-schemes.js	stages.map((s, i) => { const color = colors[i] || '#3b82f6'; return `<button data-onclick="selectKillChainStage(${s.id})
auth-ui.js	hasLevel ? `
library-home.js	(() => { const stage = findKillChainStageForBook(b); if (!stage) return ''; return `<button class="btn-detail" data-stat
library-home.js	adminStats
library-home.js	b.has_cover ? `<img src="${api.books.coverUrl(b.id)}" alt="" data-static-style="a467" data-onerror="replaceWithFallback(
library-home.js	b.has_file ? (offlineBookIds.has(b.id) ? `<button class="btn-detail offline-btn-saved" data-onclick="removeBookOffline($
library-home.js	b.rating
library-home.js	bookCategoriesText(b)
library-home.js	formatLabel
library-home.js	isAdmin ? `<button class="btn-detail" data-static-style="a469" data-onclick="openAdminBookModal(${b.id})">${ICONS.settin
onboarding.js	dateStr ? `<div data-static-style="a575">Тест пройден: ${dateStr}</div>` : ''
onboarding.js	info.icon.replace('width="22"','width="40"').replace('height="22"','height="40"').replace('width="20"','width="40"').rep
onboarding.js	result.overall_percentage
onboarding.js	topicsHtml
onboarding.js	weakHtml
reader-core.js	pdfCurrentPage
reader-core.js	pdfTotalPages
user-features.js	btn('csv', 'CSV', 'Таблица для Excel')
user-features.js	btn('json', 'JSON', 'Для импорта в другие приложения')
user-features.js	btn('md', 'Markdown', 'Текст с разметкой')
user-features.js	btn('pdf', 'PDF документ', 'Для печати и чтения')
user-features.js	inner
